groups:
  - name: guvi-app-alerts
    rules:
      - alert: GuviAppDown
        expr: up{job="guvi"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "React App is down"
          description: "React app is not responding for the last minute."

